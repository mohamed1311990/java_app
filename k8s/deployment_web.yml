apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-app-web
  labels:
    app: java-app-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java-app-web
  template:
    metadata:
      labels:
        app: java-app-web
    spec:
      containers:
        - name: java-app-web-container
          image: mohamed1311990/java-app:latest
          ports:
            - containerPort: 8080
          env:
            - name: APP_NAME
              value: "WEB"
